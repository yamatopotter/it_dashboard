# WatchIT Tower — Pendências

Único item ainda aberto após todas as fases de implementação:

---

## Segurança

- [ ] **SEC-025 — pin do next-auth@5**: quando `next-auth@5` sair do beta, atualizar `package.json` de `"^5.0.0-beta.31"` para pin exato (ex.: `"5.0.0"`) e testar login/sessão/middleware. Nota de acompanhamento já criada no dashboard.
